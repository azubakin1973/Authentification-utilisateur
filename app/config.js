/**
 * Fichier de configuration centralisé pour l'application
 * 
 * Contient les constantes globales utilisées dans toute l'application,
 * notamment l'URL de base de l'API backend.
 */

/**
 * URL de base de l'API backend
 * 
 * Note : 10.0.2.2 est l'adresse de l'hôte local (localhost) 
 * accessible depuis l'émulateur Android.
 * Pour un appareil physique, remplacez par l'adresse IP du serveur.
 */
export const API_BASE_URL = "http://10.0.2.2:3000";
